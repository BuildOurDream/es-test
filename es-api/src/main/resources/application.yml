server:
  servlet:
    context-path: /test
    
  port: 5678

  profiles:
    active: dev

spring:
  datasource:
    url: jdbc:mysql://192.168.2.151:3307/act_nx?useUnicode=true&characterEncoding=utf8&autoReconnect=true&failOverReadOnly=false&serverTimezone=GMT%2b8
    username: root
    password: 123123
    hikari:
      # 最小空闲连接数量
      minimum-idle: 8
      maximum-pool-size: 16
      # 连接池名称
      pool-name: MyHikariCP
      connection-timeout: 10000


